<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shop">
        <activity android:name=".productsMainActivity"
            android:parentActivityName=".registerMainActivity"/>
        <activity android:name=".aboutMainActivity"
            android:parentActivityName=".productsMainActivity"/>
        <activity android:name=".basketMainActivity"
            android:parentActivityName=".productsMainActivity"/>
        <activity android:name=".registerMainActivity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>